case #1: a single file inside a directory with 3 words

case #2: two files inside a directory without overlapping words

case #3: two files inside a directory with some overlapping words

case #4: argv[2] is a file

case #5 argv[1] is a directory: should crash gracefully

case #6 argv[2] doesn't exist 
